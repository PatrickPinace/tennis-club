---
// matches/add.astro — przekierowanie do historii meczów z otwartym modalem
export const prerender = false;

import { url } from '@/lib/url';

return Astro.redirect(url('/matches#add-match'));
---
